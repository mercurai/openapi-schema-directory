{
  "name": "openapi-schema-directory",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "Community-maintained OpenAPI schema directory with automated discovery and refresh.",
  "scripts": {
    "seed:apis-guru": "ts-node scripts/seed-from-apis-guru.ts",
    "discover": "ts-node scripts/discover.ts",
    "refresh": "ts-node scripts/refresh.ts",
    "fetch:all-versions": "ts-node scripts/fetch-all-versions.ts",
    "intake:community": "ts-node scripts/intake-community.ts",
    "build:index": "ts-node scripts/build-index.ts",
    "validate": "ts-node scripts/validate.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --test"
  },
  "dependencies": {
    "oas-normalize": "^11.1.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3",
    "yaml": "^2.6.1"
  }
}
